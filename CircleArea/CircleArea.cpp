/*
Circle Area
Pawelski
3/13/2023
C++
*/

#include <iostream>
#include <math.h>

int main()
{
    // declare a constant called pi with the value 3.14159
    // declare a variable called radius with the value 4
    double area = pi * pow(radius, 2);
    std::cout << "Area of the circle: " << area;
    return 0;
}
